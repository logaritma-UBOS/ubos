console.log('--- SUPABASE ACCOUNTING PATCH INSTRUCTIONS ---\n\nKarena keterbatasan akses kredensial PostgreSQL langsung, silakan Copy-Paste perintah SQL berikut ini ke fitur [SQL Editor] di Dashboard Supabase Anda untuk mengaktifkan modul Accounting:\n\n-- 1. Tambahkan kolom is_investor_view_only ke tabel merchants\nALTER TABLE merchants\n  ADD COLUMN IF NOT EXISTS is_investor_view_only BOOLEAN DEFAULT false;\n\n-- 2. Buat tabel cash_transactions\nCREATE TABLE IF NOT EXISTS cash_transactions (\n  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),\n  created_at TIMESTAMP WITH TIME ZONE DEFAULT NOW(),\n  transaction_date DATE NOT NULL DEFAULT CURRENT_DATE,\n  category VARCHAR(100) NOT NULL,\n  description TEXT,\n  type VARCHAR(10) NOT NULL CHECK (type IN (\'IN\', \'OUT\')),\n  amount NUMERIC NOT NULL DEFAULT 0,\n  proof_url TEXT\n);\n\nconsole.log(\'Skema Accounting & Investor Transparency siap dibuat via SQL Editor Supabase.\');');
