console.log('--- SUPABASE CRM PATCH INSTRUCTIONS ---\n\nKarena keterbatasan akses kredensial PostgreSQL langsung, silakan Copy-Paste perintah SQL berikut ini ke fitur [SQL Editor] di Dashboard Supabase Anda untuk menambahkan kolom pelacakan CRM:\n\nALTER TABLE merchants\n  ADD COLUMN IF NOT EXISTS ip_address VARCHAR(255),\n  ADD COLUMN IF NOT EXISTS device_info TEXT,\n  ADD COLUMN IF NOT EXISTS last_active_at TIMESTAMP WITH TIME ZONE,\n  ADD COLUMN IF NOT EXISTS current_page VARCHAR(255),\n  ADD COLUMN IF NOT EXISTS upsell_history JSONB DEFAULT ''[]''::jsonb;\n\nconsole.log(\Tabel merchants berhasil diperbarui dengan struktur CRM Tracker.\);'); node patch_crm.js
